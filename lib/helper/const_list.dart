import 'package:cars_api/helper/const_string.dart';

sealed class ConstList {
  static List data = [
    ConstString().togg,
    ConstString().tesla,
    ConstString().mercedes,
    ConstString().mazda,
    ConstString().cadilac,
    ConstString().bmw,
  ];
}
